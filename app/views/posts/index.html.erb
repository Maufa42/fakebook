
<div class = "container sample ">

<h1>Posts</h1>
<% @posts.each do |post|%>
Author:<%=link_to post.name,post_path(post)%><br>
Description: <%= post.description%><br>
<%# <% debugger %> 
<%= image_tag post.image , style:"width:720px;height:340px"%>

    <% like = current_user.likes.find_by(post: post) %>
          <% if like.nil? %>
          <%=button_to "Like",likes_path(post_id: post[:id]) %>
          <%else%>  
          <%=button_to "Unlike",likes_path(post_id: post[:id]), method: :delete %>
          <%end%>
          <%= post.likes.count %>
          <br><br><hr>
<%end%>

<h5><%= link_to 'New Post', new_post_path %>
</h5>
</div>

